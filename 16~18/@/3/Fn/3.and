3.and () 
{ 
    local D="mplayer -vo null -ao pcm:%4%FIFO, aplay -D bluealsa:SRV=...:DEV=...:PROFILE=a2dp FIFO";
    ( function 1. () 
    { 
        rm -f FIFO;
        mkfifo FIFO
    };
    function 2. () 
    { 
        mplayer -vo null -ao pcm:file=%4%FIFO $@ &
    };
    function 3. () 
    { 
        aplay -D bluealsa:SRV=org.bluealsa:DEV=$( sound-device Jam Classic ):PROFILE=a2dp FIFO
    };
    1. && 2. && 3. $@ )
}
