scp-ha-1 () 
{ 
    local D="Copy to/from ha-1 (snapshot of disk in store)";
    local a="scp -P 11022 ${1:-root@localhost:/}";
    test -n "$1" && shift && a="$a $*";
    ${a} 2> /dev/null || guix environment --ad-hoc openssh-sans-x -- ${a}
}
