ssh-ha-1 () 
{ 
    local D="Connect to ha-1 (snapshot of disk in store)";
    local a="ssh -p 11022 ${1:-root}@localhost";
    ${a} 2> /dev/null || guix environment --ad-hoc openssh-sans-x -- ${a}
}
