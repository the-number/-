start-router () 
{ 
    local D="start routing, and masquerading";
    sudo sysctl net.ipv4.ip_forward=1;
    sudo sysctl net.ipv4.conf.all.proxy_arp=1;
    sudo iptables -t nat -A POSTROUTING -o wlan0 -j MASQUERADE
}
